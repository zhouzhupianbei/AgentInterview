# 04-面试题库

> 分类整理的面试题与参考答案：基础、进阶、实战

---

## 4.1 AIGC 基础题

### 【基础】Transformer 相关

#### Q1: Transformer 中 Self-Attention 的计算复杂度是多少？

**答案**：O(n² · d)

- n：序列长度
- d：模型维度

**推导**：
```
QK^T: (n×d) × (d×n) = O(n²·d)
softmax(QK^T): O(n²)
attention·V: (n×n) × (n×d) = O(n²·d)
总计：O(n²·d)
```

**优化方案**：
- FlashAttention：IO 感知，减少 HBM 访问
- Sparse Attention：只计算部分 attention
- Linear Attention：用 kernel trick 降为 O(n)

---

#### Q2: LayerNorm 和 BatchNorm 的区别？

**答案**：

| 维度 | BatchNorm | LayerNorm |
|------|-----------|-----------|
| 归一化维度 | batch 维度 | 特征维度 |
| 依赖 batch size | 是 | 否 |
| 训练/测试差异 | 需要 running stats | 一致 |
| 适用场景 | CNN | Transformer/RNN |

**代码对比**：
```python
# BatchNorm: 对 batch 维度统计
mean = x.mean(dim=0)  # [batch, seq, d] → [d]

# LayerNorm: 对特征维度统计
mean = x.mean(dim=-1, keepdim=True)  # [batch, seq, d] → [batch, seq, 1]
```

---

### 【基础】大模型相关

#### Q3: LLaMA 使用了哪些改进？

**答案**：

1. **RoPE 位置编码**：更好的长度外推性
2. **SwiGLU 激活**：替代 ReLU，效果更好
3. **Pre-Normalization**：LayerNorm 放在子层前
4. **RMSNorm**：简化版 LayerNorm，更快

**SwiGLU 公式**：
```
SwiGLU(x) = Swish(xW) ⊗ (xV)
其中 Swish(x) = x · sigmoid(βx)
```

---

#### Q4: 什么是 KV Cache？如何节省显存？

**答案**：

**KV Cache**：缓存已生成 token 的 K/V，避免重复计算

**显存计算**：
```
对于 LLaMA-7B (32 层，d=4096, 32 heads):
每层每 token 的 KV cache:
  2 × 32 heads × (4096/32) × 4 bytes = 32KB
  
生成 1000 tokens:
  32 层 × 32KB × 1000 = 1GB
```

**优化方法**：
1. **量化**：FP16 → INT8，节省 50%
2. **MQA/GQA**：共享 K/V heads
3. **Offload**：部分 cache 放到 CPU 内存

---

## 4.2 AI Agent 题

### 【进阶】Agent 设计

#### Q5: 设计一个能帮你写周报的 Agent，如何设计？

**参考答案**：

```
1. 需求分析
   - 输入：本周工作内容（零散）
   - 输出：结构化周报（成果、问题、计划）

2. 工具设计
   - 日历 API：获取会议记录
   - Git API：获取代码提交
   - 文档模板：周报格式

3. Agent 流程
   Thought: 需要收集本周工作数据
   Action: get_calendar_events[2026-03-17, 2026-03-22]
   Action: get_git_commits[since=last_week]
   Observation: 获得 5 个会议、12 次提交
   
   Thought: 需要分类整理
   Action: categorize_work[events, commits]
   
   Thought: 生成周报
   Action: generate_report[categorized_work, template]

4. 记忆
   - 历史周报（保持风格一致）
   - 领导关注点

5. 人工确认
   - 生成草稿 → 用户修改 → 最终版
```

---

#### Q6: Agent 执行工具调用失败如何处理？

**参考答案**：

**1. 错误分类**：
```python
class ToolError(Exception):
    NETWORK_ERROR      # 网络问题
    API_ERROR          # API 返回错误
    TIMEOUT_ERROR      # 超时
    RATE_LIMIT_ERROR   # 限流
    INVALID_INPUT      # 输入无效
```

**2. 处理策略**：
```python
def handle_tool_error(error, attempt):
    if error == NETWORK_ERROR and attempt < 3:
        return RETRY_WITH_BACKOFF
    elif error == RATE_LIMIT_ERROR:
        return WAIT_AND_RETRY
    elif error == INVALID_INPUT:
        return ASK_USER_FOR_CORRECTION
    else:
        return FALLBACK_TO_ALTERNATIVE
```

**3. 降级方案**：
- 切换到备用 API
- 使用缓存数据
- 请求人工介入

---

### 【实战】多 Agent 协作

#### Q7: 如何设计一个多 Agent 代码审查系统？

**参考答案**：

```
角色设计：
1. 风格审查员 (StyleReviewer)
   - 检查代码规范（PEP8、命名）
   - 工具：pylint、black

2. 安全审查员 (SecurityReviewer)
   - 检测安全漏洞
   - 工具：bandit、semgrep

3. 性能审查员 (PerformanceReviewer)
   - 识别性能瓶颈
   - 工具：profiler

4. 主审查员 (LeadReviewer)
   - 汇总各审查员意见
   - 生成最终报告
   - 与开发者对话

协作流程：
PR → LeadReviewer → 分发 → 各审查员并行审查
                              ↓
                     汇总 → 生成报告 → 开发者
```

**代码示例**：
```python
from autogen import AssistantAgent, GroupChat, GroupChatManager

style_agent = AssistantAgent("StyleReviewer", system_message="你负责检查代码规范...")
security_agent = AssistantAgent("SecurityReviewer", system_message="你负责检查安全...")
performance_agent = AssistantAgent("PerformanceReviewer", system_message="你负责检查性能...")
lead_agent = AssistantAgent("LeadReviewer", system_message="你负责汇总...")

groupchat = GroupChat(
    agents=[style_agent, security_agent, performance_agent, lead_agent],
    messages=[]
)
manager = GroupChatManager(groupchat=groupchat, llm_config=llm_config)

user_proxy.initiate_chat(manager, message="审查这个 PR: [代码]")
```

---

## 4.3 RAG 题

### 【进阶】RAG 优化

#### Q8: RAG 检索结果不相关怎么办？

**参考答案**：

**1. Query 优化**：
```python
# Query 改写
def rewrite_query(query):
    prompt = f"""
    原始问题：{query}
    请改写为更适合检索的形式，提取关键实体和意图。
    """
    return llm.generate(prompt)

# Query 扩展
def expand_query(query):
    synonyms = get_synonyms(query)  # 同义词
    related = get_related_terms(query)  # 相关词
    return [query] + synonyms + related
```

**2. 检索优化**：
```python
# 混合检索（向量 + 关键词）
retriever = EnsembleRetriever(
    retrievers=[bm25_retriever, vector_retriever],
    weights=[0.3, 0.7]
)

# MMR 去重
retriever = vectorstore.as_retriever(
    search_type="mmr",
    search_kwargs={"k": 5, "lambda_mult": 0.5}
)
```

**3. 后处理**：
```python
# 重排序
from langchain.retrievers.document_compressors import LLMChainExtractor

compressor = LLMChainExtractor.from_llm(llm)
compressed_docs = compressor.compress_documents(docs, query)
```

---

#### Q9: 如何处理长文档的 RAG？

**参考答案**：

**1. 分层索引**：
```
文档 → 章节 → 段落 → 句子
  ↓      ↓      ↓      ↓
Embed  Embed  Embed  Embed

检索：先检索章节，再检索段落
```

**2. 父子文档**：
```python
# 父文档（大 chunk，用于检索）
# 子文档（小 chunk，用于生成）

parent_docs = split(large_chunk_size=1000)
child_docs = split(small_chunk_size=200)

# 检索到父文档，返回对应的子文档
```

**3. 摘要索引**：
```python
# 为每个文档生成摘要
summary = llm.generate(f"总结文档：{doc}")

# 检索摘要，返回原文档
```

---

## 4.4 开发实战题

### 【实战】系统设计

#### Q10: 设计一个支持 10 万 DAU 的 AI 问答系统

**参考答案**：

```
1. 架构设计
   ┌─────────────┐
   │   Client    │
   └──────┬──────┘
          │
   ┌──────▼──────┐
   │  Load Balancer │
   └──────┬──────┘
          │
   ┌──────▼──────┐
   │  API Gateway  │
   └──────┬──────┘
          │
   ┌──────▼──────┐     ┌──────────────┐
   │ App Server  │────▶│ Vector DB    │
   │ (10 实例)    │     │ (Chroma 集群) │
   └──────┬──────┘     └──────────────┘
          │
   ┌──────▼──────┐     ┌──────────────┐
   │ LLM Cluster │────▶│ Cache (Redis)│
   │ (vLLM×4)    │     └──────────────┘
   └─────────────┘

2. 性能估算
   - DAU: 10 万
   - 人均提问：10 次/天
   - 总请求：100 万/天
   - 峰值 QPS: 100 万 / 86400 × 3 = ~35 QPS
   
   单实例 vLLM 吞吐：50 req/s
   需要实例：35 / 50 = 1 个（考虑冗余×4）

3. 优化策略
   - 缓存：常见问题缓存（命中率 60%）
   - 批处理：动态 batching
   - 降级：高峰期用小模型
   - 限流：单用户 10 req/min

4. 监控告警
   - 延迟 P99 < 2s
   - 错误率 < 1%
   - GPU 利用率 < 80%
```

---

### 【实战】代码题

#### Q11: 实现一个简单的 RAG 系统

**参考答案**：

```python
from langchain.embeddings import OpenAIEmbeddings
from langchain.vectorstores import Chroma
from langchain.chat_models import ChatOpenAI
from langchain.chains import RetrievalQA

class SimpleRAG:
    def __init__(self, docs_dir="./docs"):
        # 1. 加载文档
        self.documents = self.load_docs(docs_dir)
        
        # 2. 创建向量库
        self.embeddings = OpenAIEmbeddings()
        self.vectorstore = Chroma.from_documents(
            documents=self.documents,
            embedding=self.embeddings,
            persist_directory="./chroma"
        )
        
        # 3. 创建 QA chain
        self.llm = ChatOpenAI(model="gpt-4", temperature=0)
        self.qa_chain = RetrievalQA.from_chain_type(
            llm=self.llm,
            retriever=self.vectorstore.as_retriever(search_kwargs={"k": 3})
        )
    
    def load_docs(self, docs_dir):
        from langchain.document_loaders import DirectoryLoader
        loader = DirectoryLoader(docs_dir, glob="**/*.md")
        return loader.load()
    
    def query(self, question):
        result = self.qa_chain({"query": question})
        return {
            "answer": result["result"],
            "sources": [doc.page_content for doc in result["source_documents"]]
        }

# 使用
rag = SimpleRAG()
response = rag.query("什么是 Transformer？")
print(response["answer"])
```

---

#### Q12: 实现 Agent 的工具调用解析

**参考答案**：

```python
import json
import re

class ToolCaller:
    def __init__(self):
        self.tools = {
            "search": self.search_web,
            "calculate": self.calculate,
            "get_weather": self.get_weather
        }
    
    def parse_and_call(self, llm_response):
        """
        解析 LLM 输出并调用工具
        格式：Action: tool_name[arguments]
        """
        pattern = r"Action:\s*(\w+)\[(.*?)\]"
        match = re.search(pattern, llm_response)
        
        if not match:
            return None, "No action found"
        
        tool_name, args_str = match.groups()
        
        if tool_name not in self.tools:
            return None, f"Unknown tool: {tool_name}"
        
        try:
            args = json.loads(args_str)
            result = self.tools[tool_name](**args)
            return result, None
        except Exception as e:
            return None, f"Error: {str(e)}"
    
    def search_web(self, query):
        # 实现搜索逻辑
        return f"搜索结果：关于{query}的信息..."
    
    def calculate(self, expression):
        return eval(expression)  # 注意：生产环境需要沙箱
    
    def get_weather(self, city):
        return f"{city}今天天气晴朗，25°C"

# 使用
caller = ToolCaller()
llm_output = """
Thought: 我需要搜索信息
Action: search_web[{"query": "AI Agent 教程"}]
"""
result, error = caller.parse_and_call(llm_output)
```

---

## 4.5 行为面试题

### 【软技能】

#### Q13: 描述一个你解决过的最有挑战的 AI 项目

**回答框架 (STAR)**：

```
Situation（情境）:
- 项目背景、团队规模、时间线

Task（任务）:
- 你的职责、目标

Action（行动）:
- 你采取的具体行动
- 技术选型、架构设计
- 遇到的困难及解决

Result（结果）:
- 量化成果（性能提升、成本降低）
- 学到的经验
```

**示例**：
```
S: 公司需要构建智能客服系统，处理 10 万 + 日咨询
T: 我负责 RAG 系统设计和优化
A: 
  - 选择了 Chroma + GPT-4 方案
  - 优化检索：混合检索 + 重排序，准确率从 70%→90%
  - 实现缓存机制，响应时间从 3s→0.8s
R: 
  - 上线后人工客服工作量减少 60%
  - 用户满意度提升 25%
  - 年节省成本 200 万
```

---

#### Q14: 如何跟进 AIGC 领域的快速发展？

**参考答案**：

```
1. 信息渠道
   - 论文：arXiv daily check (cs.CL, cs.LG)
   - 博客：OpenAI、Anthropic、Hugging Face
   - 社区：Twitter、Reddit r/MachineLearning
   - 中文：知乎 AI 话题、微信公众号

2. 实践方式
   - 每周复现一篇论文
   - 参与开源项目
   - 写技术博客总结

3. 学习方法
   - 费曼技巧：尝试向他人解释
   - 项目驱动：用新技术做项目
   - 建立知识体系：思维导图
```

---

## 4.6 高频算法题

### 【LeetCode 高频】

#### Q15: 实现 Top K 问题（堆）

```python
import heapq

def topKFrequent(nums, k):
    """
    LeetCode 347. Top K Frequent Elements
    """
    # 统计频率
    from collections import Counter
    count = Counter(nums)
    
    # 维护大小为 k 的小顶堆
    heap = []
    for num, freq in count.items():
        if len(heap) < k:
            heapq.heappush(heap, (freq, num))
        elif freq > heap[0][0]:
            heapq.heapreplace(heap, (freq, num))
    
    return [num for freq, num in heap]

# 时间复杂度：O(n log k)
# 空间复杂度：O(n)
```

---

#### Q16: 实现 Attention 机制

```python
import torch
import torch.nn as nn
import torch.nn.functional as F

class ScaledDotProductAttention(nn.Module):
    def __init__(self, d_model):
        super().__init__()
        self.scale = d_model ** 0.5
    
    def forward(self, Q, K, V, mask=None):
        # Q, K, V: [batch, seq_len, d_model]
        scores = torch.matmul(Q, K.transpose(-2, -1)) / self.scale
        
        if mask is not None:
            scores = scores.masked_fill(mask == 0, -1e9)
        
        attn = F.softmax(scores, dim=-1)
        output = torch.matmul(attn, V)
        
        return output, attn

# 使用
attention = ScaledDotProductAttention(d_model=512)
output, attn_weights = attention(Q, K, V)
```

---

## 📚 更多题目

参考资源：
- [WeThinkIn/AIGC-Interview-Book - 算法岗面试求职宝典](https://github.com/WeThinkIn/AIGC-Interview-Book/tree/main/%E7%AE%97%E6%B3%95%E5%B2%97%E9%9D%A2%E8%AF%95%E6%B1%82%E8%81%8C%E5%AE%9D%E5%85%B8)
- [WeThinkIn/AIGC-Interview-Book - 大厂高频算法题](https://github.com/WeThinkIn/AIGC-Interview-Book/tree/main/%E5%A4%A7%E5%8E%82%E9%AB%98%E9%A2%91%E7%AE%97%E6%B3%95%E9%A2%98)

---

**上一章**：[03-开发技能.md](./03-开发技能.md)  
**下一章**：[05-项目建议.md](./05-项目建议.md)
